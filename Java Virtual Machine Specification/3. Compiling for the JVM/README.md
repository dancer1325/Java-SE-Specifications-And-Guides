* Goal
  * compiler
    * compilation of code / written in Java -- to -- instruction set of JVM
* JVM
  * -- designed to support -- Java programming language 
* JDK
  * _Example:_ Oracle
  * == compiler + runTime + ...
    * 👁️runTime -- implements the -- JVM itself 👁️
* compiler vs translator
  * translator
    * instruction set of JVM -- to the -- instruction set of specific CPU
    * _Example:_ JIT (just-in-time) code generator